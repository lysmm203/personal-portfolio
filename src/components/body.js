

export default function Body({children}){

    return (
        <div className={"wrapper"}>
            <div className={"padding-div"}>
                {children}
            </div>
        </div>
    )

}